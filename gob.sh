#!/bin/sh
echo "Gob's Program: Y/N?"
read yesno
if [ "$yesno" == "Y" ]; then
  for (( i = 0 ; i <= 10; i-- ))
	do
  	    echo "PENUS PENUS PENUS PENUS PENUS PENUS"
	done
else
	echo "Go away, Michael!"
	for (( i = 0 ; i <= 10; i-- ))
	do
            echo "PENUS PENUS PENUS PENUS PENUS PENUS"
	done
fi
